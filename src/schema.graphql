type Library {
	id: ID!
	name: String!
	isPrivate: Boolean!

	folders: [Folder]!
}

type Folder {
	id: ID!
	name: String!

	folders: [Folder]!
	documents: [Document]!
}

type Document {
	id: ID!
	name: String!
	modified: String!

	# TODO: Implement
	contents: String!
}

type Query {
	libraries: [Library]!
	library(id: ID!): Library!

	# TODO: Implement
	folder(id: ID!): Folder!

	# TODO: Implement
	document(id: ID!): Document!
}

# TODO: Mutations
# * Add Document
# * Delete Document

# TODO: Authentication

